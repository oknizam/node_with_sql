# IAM 

1. IAM used to create multiple users for diffrent access
2. We can create alias in maian account
3. If i created an aws account which is root user, i want to give access of my account to other user (member) 
4. we go to IAM create alias user
5. when we create alias user alias userid and alias url so that IAM user can access to my account
6. I am working for a company , root user will be company admin account
7. for developers there will be IAM account
8. So we can create an account as IAM user
9. Give permissions to access only few required services to that user
10. IAM usre can't access all services 
11. When we give permssions (plicies) to IAM users , their is limit for Policies (Max 10)
12. But i want to give morethan 10 policies is not allowed 

13. User Groups

  1. Create user group
  2. Attach policies 
  3. Add user to that group
  4. Now user will have access to all group policies

14. IAM is used to create users with selected permissions, so they can also have access aws services based policies
15. There will be an root uses which is individual or organization
16. If we want to give few permissions like only s3 access to new user we will create an IAM user
17. Create groups which can have ploicies (Created deployment group , added s3 full access permission)
18. Add user into those groups so that user who is part of that group can have access to s3


19. We can create our own policies (custom policy)



